# ─── Python ───
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ─── Virtual environments ───
.venv/
venv/
env/
ENV/
.conda/
# conda envs are usually outside the repo, but ignore local ones:
envs/

# ─── Jupyter ───
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py
# Optional: strip notebook outputs from git instead of ignoring notebooks —
# see nbstripout note below. Do NOT blanket-ignore *.ipynb (you want them tracked).

# ─── Testing / coverage / type checkers ───
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# ─── IDE / OS ───
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ─── Secrets / credentials (IMPORTANT for this project) ───
.env
.env.*
*.key
*.pem
credentials.json
service_account*.json
.config/earthengine/          # Earth Engine auth tokens
*_token*
secrets/

# ─── This project's heavy / generated artifacts ───
feat_tiles/                   # zarr tile cache from get_feature_stack
*.zarr/
*.zarr
feat_cube*.zarr/
*.tif
*.tiff
# downloaded / produced pairs — regenerable, often large:
*.geojson
*.parquet
disturbed_pairs*.geojson
# but KEEP small input fixtures you want tracked (un-ignore them):
!standard_scaler.csv

# ─── Logs / scratch ───
*.log
scratch/
tmp/
